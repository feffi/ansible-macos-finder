---
galaxy_info:
  author: Kevin Wennemuth
  description: Ansible role to manage finder settings in macOS.
  license: MIT
  min_ansible_version: 2.4.0.0
  platforms:
    - name: Darwin
      versions:
        - all
  galaxy_tags:
    - system
    - osx
    - macos
    - config
    - finder
dependencies:
  - { role: feffi.macos-defaults,
      macos_defaults: {
        defaults: []
      }
    }
